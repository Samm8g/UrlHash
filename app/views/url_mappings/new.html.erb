<h1>Hash a URL</h1>

<%= form_with(model: @url_mapping) do |form| %>
  <% if @url_mapping.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@url_mapping.errors.count, "error") %> prohibited this url_mapping from being saved:</h2>

      <ul>
        <% @url_mapping.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :original_url %>
    <%= form.text_field :original_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
